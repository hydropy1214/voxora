# Agents

## Cursor Cloud specific instructions

This repository (`voxora`) is currently an empty project scaffold — it contains only a `README.md`. There are no source files, dependencies, build scripts, tests, or services.

- **No package manager lockfile or dependency manifest exists.** When code is added, update this section with the correct install/build/run/test commands.
- **No lint, test, or build tooling is configured.** Future agents should check for newly added tooling before attempting those steps.
- **No services to start.** There are no backend, frontend, or database services defined yet.

When the first real code lands, update this file with:
1. How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
2. How to run lint / tests / build / dev server.
3. Any non-obvious caveats discovered during setup.
